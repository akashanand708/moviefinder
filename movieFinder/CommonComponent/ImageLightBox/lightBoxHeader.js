import React from 'react';
import { View, Text } from 'react-native';

const LightBoxHeader = (props) => {
    let { } = props;
    let { } = state;
    return (
        <View><Text style={{ color: '#ffffff' }}>Close</Text></View>
    );
}

export default LightBoxHeader; 